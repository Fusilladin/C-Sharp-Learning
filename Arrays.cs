using System;
class Program {
  static void Main(string[] args)
  {
    int[]luckyNumbers = {4,8,15,16,28,43}; // Make array
    string[]friends = new_string[5]; // the number of indexes the str can hold
    friends[0] = "Jim"; // labeling the indexes in the array
    friends[1] = "Kelly';"
    
    luckyNumbers[1] = 900 // can change an index of an array at any time
    
    Console.WriteLine(luckyNumbers[1]); // access an index in the array
    
    Console.ReadLine();
  }
}